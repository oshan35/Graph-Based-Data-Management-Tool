#include<iostream>
#include "QueryDecoder.h"
using namespace std;

int main(){
    // string query = "select name from cluster1 where age>20";
    // string queryType = QueryDecoder::queryType(query);
    // cout<<"Query code: "<<queryType<<endl;
    // QueryDecoder::queryDecoder(queryType,query);

    return 0;



}
